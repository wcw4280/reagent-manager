name: Keep Supabase Alive

on:
  schedule:
    - cron: "15 0 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase REST API
        run: |
          curl -sS -f \
            "${{ secrets.SUPABASE_URL }}/rest/v1/reagents?select=id&limit=1" \
            -H "apikey: ${{ secrets.SUPABASE_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_KEY }}"
